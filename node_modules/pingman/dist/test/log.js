"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const ava_1 = __importDefault(require("ava"));
const log_1 = __importDefault(require("../src/log"));
// const fs = require('fs');
ava_1.default('Check if log file is created without logging enabled', t => {
    const log = new log_1.default('log.txt', false);
    t.deepEqual(log.logFile, {});
    t.is(log.enabledLogging, false);
});
ava_1.default('Check if log file is created and can be written to', async (t) => {
    const log = new log_1.default('log.txt', true);
    const testData = 'Test';
    await log.writeToLogFile(testData);
    t.is(log.enabledLogging, true);
});
