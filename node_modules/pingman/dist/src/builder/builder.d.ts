import { commandBuilder, extendedPingOptions } from '../types';
declare function createBuilder(ip: string, platform: string, options?: extendedPingOptions): commandBuilder;
export default createBuilder;
